{
  "name": "bend-fastify-2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w",
    "start": "fastify start -w -l info -P -a 0.0.0.0 ./dist/app.js",
    "dev": "npm run build && concurrently -k -p \"[{name}]\" -n \"TypeScript,App\" -c \"yellow.bold,cyan.bold\" \"npm:watch\" \"npm:dev:start\"",
    "dev:start": "fastify start --ignore-watch=.ts$ -w -l info -P dist/app.js --host"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/autoload": "^5.8.0",
    "@fastify/sensible": "^5.5.0",
    "@fastify/view": "^8.2.0",
    "fastify": "^4.24.3",
    "fastify-cli": "^5.8.0",
    "fastify-plugin": "^4.5.1",
    "pug": "^3.0.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.9",
    "@types/tap": "^15.0.10",
    "concurrently": "^8.2.2",
    "fastify-tsconfig": "^2.0.0",
    "tab": "^1.0.0",
    "ts-node": "^10.9.1",
    "tsx": "^3.14.0",
    "typescript": "^5.2.2"
  }
}
